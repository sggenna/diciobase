# Optional: use Supabase as the demo data source (no separate API).
# Set these when deploying the demo on Supabase.
# REACT_APP_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
# REACT_APP_SUPABASE_ANON_KEY=your_anon_key

# Optional: override API URL in production (defaults to Firebase Functions).
# REACT_APP_API_URL=https://us-central1-diciobase.cloudfunctions.net
